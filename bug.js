const function(stringValue, stringLength) {
    let result = ''
    for (let i = 0; i < stringLength; i++ ){
        result = stringValue
    }

    if (stringLength < 0 < 1){
        return 'ERROR'
    }
return result
};
